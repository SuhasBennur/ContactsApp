<UserControl x:Class="ContactsApp.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContactsApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="425" d:DesignWidth="400">
    <Grid Background="#FF0C0D0E">
        <StackPanel Margin="10">
            <TextBlock x:Name="Nameblock" Text="{Binding Name}" FontSize="30" FontWeight="Bold" Foreground="White" />
            <TextBlock x:Name="Emailblock" Text="{Binding Email}" FontSize="25" Foreground="Silver" />
            <TextBlock x:Name="PhoneBlock" Text="{Binding Phone}" FontSize="15" FontStyle="Italic" Foreground="WhiteSmoke" />
        </StackPanel>
    </Grid>
</UserControl>
